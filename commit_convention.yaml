## commit_convention.yml
## omit the commit_types to use the default angular-style commit types
# commit_types:
#   - type: description of what the short-form "type" means
scopes:
  android: changes in /android, doesn't effect whole application
  linux: changes in /linux, doesn't effect whole application
  web: changes in /web, doesn't effect whole application
